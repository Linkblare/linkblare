import { LinkContent } from '@/schema/item-schema'
import React from 'react'

const VideoItemRenderer = ({
    thumbnail,
    content
}: {
    thumbnail?: string,
    content: LinkContent
}) => {
  return (
    <div>VideoItemRenderer</div>
  )
}

export default VideoItemRenderer